# Verdify Platform Coordination Requests

Last updated: 2026-07-14

Agent name: `verdify-platform`

Use this file for out-of-lane asks. Keep each request scoped to the minimum
action and create/update a GitHub issue when the request blocks delivery.

| Requesting lane | Target owner | Needed action | Blocking? | Deadline | Minimal access/action |
|---|---|---|---|---|---|
| L1 Architecture Audit (#343) | `storage-infra` | Confirm current storage ownership for prod DB PVCs, backup PVCs, NAS/Synology storage paths, and CNPG/PITR prerequisites. | Yes for storage readiness claims | Before closing G0/G2 storage sections | Read-only PVC/PV/StorageClass status and backup policy |
| L1 Architecture Audit (#343) | `network-infra` | Confirm public route ownership for `*.verdify.ai`, app Traefik routes, Cloudflare tunnel path, ESP32/device VLAN egress gates, and Frigate/Home Assistant route assumptions. | Yes for route/device-path changes | Before ingress, device-network, or L7 integration changes | Named route/firewall/status review only |
| L6 Observability (#348) | `monitoring-stack` | Confirm shared Grafana/Prometheus/alert ownership versus repo-owned app dashboard manifests and drift/data-hole alert responsibilities. | No for docs; yes for shared telemetry changes | Before L6 closure | Dashboard/alert inventory for Verdify resources |
| L2/L3/L7/L8/L10 | Jason | Approve any firmware OTA, prod ArgoCD sync touching the live writer, device VLAN action, prod-destructive DB operation, credential rotation, or outward-facing DNS/edge/org change. | Yes for gated operations | Before action | Explicit approval; no standing broad access |
| L8 Irrigation (#350) | Jason / horticulture decision | Decide fertilizer-tank routing: wall drip/fertigation loop versus orchids/manual care, including whether fish-based fertilizer belongs in automated loop. | Yes for L8 implementation | Before irrigation/fertilization control changes | Human horticulture decision and physical routing confirmation |
| L7 Lighting/Occupancy (#349) | Jason / integration owners | Choose/confirm the occupancy event path to firmware: MQTT, Home Assistant API, direct Frigate, or Kubernetes-local bridge. | Yes for L7 implementation | Before occupancy firmware/service changes | Contract review; no credential values |
| Pass-1 storage provisioning (#533; storage-infra#53) | Jason / storage-infra | **Blocks approved activation Pass 1:** create private Garage bucket `verdify-lab-occurrences`, distinct bucket-scoped reader/writer identities, out-of-band delivery to the contracted Secret names/keys, anonymous denial, reader-write denial, conditional write/delete, and prefix isolation. Full provisioning contract and native dependencies are #533; provider-side request is `jvallery/storage-infra#53`. Packed capacity and real-endpoint integration belong to #537/#540 after delivery. | Yes — blocks #540/#541 | Next attended window | Garage admin + Secret delivery; values never in git/GitHub/chat/logs/evidence |
| Pass-1 isolated reporting resource (#534) | `verdify-platform` executor (Jason-approved) | Execute the anonymous-disabled rendering tier, one-way allowlisted read-only reporting feed, source watermark, and credential distinct from all Track A roles/datasources under the recorded stage-only approval. Full What/Why/How/Test/Success and boundaries are #534; no separate provider queue owns it. | Yes — blocks #541 live proof, not #535 source | Next attended window | Reporting projection/tier + separate credential delivery by name/key only |
| Lab-stage occurrence activation (#541; #476/#480) | Jason / `verdify-platform` executor | **APPROVED 2026-07-14 for the exact stage-only scope.** Source and protected delivery remain independent gates: #533/#534/#535/#537/#540/#542 must be green first. Each pass freezes an immutable Platform commit/pin-set and rollback revision, uses a reviewed `jvallery/agents` exact-revision actuator, requires Argo `Synced` + `Healthy` with the reviewed inventory, and restores `targetRevision: main` plus manual/no-prune posture after T+10. Pass 1 activates reporting + producer + two-pod runtime unrouted; Pass 2 changes the stage route only after 143+2/both-cache convergence, followed by the 24h/96-sample soak. Scope excludes production, DNS/edge, Quartz, devices/private networks, and Track A credentials. | Yes — approval is recorded; prerequisites still block execution | After all #541 blockers pass | Exact-revision stage actuator and precomputed rollback only; no broader authority |
| Gated release manifest (2026-07-13 evening) | Jason | **One attended window ships all of today’s backend value:** (1) gated `argocd app sync verdify-prod-dark` rolls the pinned digests — verdify-mcp (outcome_kpi parallelization #497 + snapshot cycle counts #503), verdify-api, verdify-ingestor (RAG corpus repoint #499 — restores Iris RAG freshness), setpoint-server pin correction #506 (prevents rollback), vision CronJob GitOps ownership #508; (2) apply migrations **201** then **202** per rollback-safety tooling, then bounce `verdify-mcp` — unlocks the ~62-91× scorecard speedup and un-breaks the timing-out outcome_kpi cycle axis; (3) delete wedged job `verdify-vision-29730060` (row above). All items verified/CI-green on main; sequencing documented in each PR. | Yes — several prod behaviors frozen until sync | Next attended window | Standard gated sync + 2 serialized migrations + 1 job deletion |
| Vision pipeline (#436, closed) | Jason | **Wedged Job blocks all vision runs since 2026-07-11T21Z:** `verdify-vision-29730060` in `verdify-prod` is active 44h with zero pods; `concurrencyPolicy: Forbid` silently skips every schedule. One gated command clears it: `kubectl delete job verdify-vision-29730060 -n verdify-prod` (next CronJob tick then runs normally on the GitOps-owned manifest from PR #508). | Yes — vision evidence frozen | Next attended window | Single job deletion; no other change |
| Legacy Quartz Lab publisher (#351) | Secret-delivery owner | Verify the existing production Quartz publisher Secret only if legacy-path diagnostics require it. The new Astro stage path must use #533's separate reader/writer resources and may not reuse this credential. | No for Astro source; yes for legacy incident diagnostics | As needed until #482 retirement | Secret metadata/status only; never values |
| L1/L5 | Secret-delivery owner | Verify prod Secret presence by name/key in `verdify-prod` without exposing values. | Yes before live readiness claims | Before secret audit closure | Secret metadata/status only |
| L1 Architecture Audit (#343) | `monitoring-stack` | Add (or confirm the split-brain alarm #241 already covers) an OUT-OF-BAND alert on `sum(verdify_esp32_writer_estab) == 0` (writer absent) and on `time() - max(climate.ts)` (telemetry stall), Slack-routed independently of the ingestor. The audit's P0: the in-repo staleness monitor runs INSIDE the writer, so a dead writer self-silences. This repo ships no PrometheusRules; the rule belongs in monitoring-stack. | Yes — P0 reliability gap | Before G2 observability closure | Add/confirm a PrometheusRule + Alertmanager route; share the rule name/expr |
| L1/L5 reliability (#343/#347) | `storage-infra` / Jason | **2026-06-17 iSCSI target-count exhaustion (P0).** `synology-csi-controller-0` logs show `Number of target reach limit` failing NEW iSCSI provisioning on BOTH `/volume1` and `/volume2` (verified — NOT an SSD-tier hardware death). Reclaim orphaned iSCSI targets from deleted PVCs and curb ephemeral CI iSCSI churn (`agent-fleet-ci` `*-workdir` PVCs consume targets). The live `verdify-db` (synology-iscsi-ssd) and `verdify-ingestor-state` (synology-iscsi) ride established sessions; a reschedule that must create a target would wedge. Also raise/confirm the DSM target cap. | Yes — acute single-writer + DB-availability risk | Immediate | DSM iSCSI target inventory + reclamation; no app changes |
| L1/L5 reliability (#343/#347) | Jason | **DB substrate retier — DONE 2026-06-17.** `verdify-db` STS recreated onto `synology-iscsi-ssd` + nodeSelector `kubernetes.io/os: linux`, reusing the existing Bound PV (no new iSCSI target → not blocked by the cap); git reconciled to the live template. Remaining DB-reliability work is PITR (next row) + the target-cap fix above. | Resolved | — | (done; see SERVICE_MAP) |
| L1/L5 reliability (#343/#347) | `storage-infra` / Jason | **DB PITR / replica (P0, still open).** `verdify-db` is a single-replica StatefulSet, nightly `pg_dump` only (RPO ≤24 h, RTO unpracticed). Re-arm CNPG (1+2 sync + Barman) OR interim WAL archiving to the `verdify-db-dumps` NFS PVC (RPO→~5 min, no new iSCSI) + a quarterly restore drill. WAL-to-NFS is the lower-risk interim and is NOT blocked by the iSCSI cap, but `archive_mode=on` needs a DB **restart** → attended maintenance window, not an unattended overnight change to the live greenhouse data plane. | Yes — top reliability gap | Before G2 closure | DB config + restore drill (attended) |
| Storage migration (operator lane) | — | **DONE 2026-06-17 (laptop-root, on Jason's deploy clearance).** Realized the hermes node-local migration (recreated the PVC), reconciled git==live for DB+lab (`2052fc2`), fixed the grafana PDB Job-selector bug (`36382e9`), and ran `argocd app sync verdify-prod-dark` → **Synced/Healthy**. The ingestor `emptyDir → durable PVC` revert was deployed in the same pass. DB STS sync was metadata-only (no roll); greenhouse datapath stayed fresh throughout. | Resolved | — | (done) |

## Resolved / No Current Blocker

| Area | Resolution |
|---|---|
| Project Board | Exact `verdify-platform` org Project #5 exists. The 2026-06-16 replan added milestones G0-G3 and issue cards #343-#352 with field metadata. Revisit only if project permissions drift. |

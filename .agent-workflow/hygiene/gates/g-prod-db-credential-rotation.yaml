schema_ref: human-gate.schema.yaml
kind: HumanGate
schema_version: "1.0"
gate_id: g-prod-db-credential-rotation-20260709
sprint_id: software-recovery-2026-07-09
lane_id: null
type: repo_hygiene
status: approved
question: May the agent rotate the production Verdify application database credential after removing the committed fallback, update the existing Kubernetes/DB consumers through the approved secret path, restart affected services, and verify rollback/connectivity without exposing the value?
owner: Jason Vallery
evidence_required:
  - Redacted gitleaks/source scan showing four standalone renderer/writer scripts carried committed password fallbacks while the snapshot client implicitly loaded the retired /srv/verdify/.env path, plus the related historical findings.
  - Boolean-only comparison proving the committed fallback matched the live verdify-app-secrets POSTGRES_PASSWORD value.
  - Source diff removing every hardcoded fallback and requiring VERDIFY_DSN or POSTGRES_PASSWORD.
allowed_decisions:
  - "rotate-now: authorize scoped production database credential rotation, consumer updates/restarts, connectivity proof, and invalidation of the committed value."
  - "defer-with-release-block: remove source fallback now but block production recovery release until Jason performs or authorizes rotation."
decision: rotate-now
rationale: >-
  Jason's 2026-07-10 completion objective explicitly grants full authority to
  complete the proposed work and production deployment. The attached scope
  names issue #438 as required work. This authorizes only the scoped rotation,
  consumer restarts, redacted connectivity proof, and old-credential
  invalidation described by the approved runbook; no other secret, DNS, edge,
  destructive database, or policy action is authorized.
opened_at: "2026-07-09T21:33:00Z"
resolved_at: "2026-07-10T12:21:20Z"
resume_state: Resume repo-hygiene verification, add a redacted historical-scan baseline only after invalidation, complete sprint planning, and keep raw credential values out of every command output and artifact.

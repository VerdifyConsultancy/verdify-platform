# Maintenance checkpoint — verdifyconsultancy-verdify-platform-codex @ 2026-07-24T21:48:18Z

- **Checkpoint #**: 1
- **Trigger**: ONYX maintenance; node7 shutting down
- **Boundary**: safe and idle; no command, deployment, merge, sync, or sub-agent work remains in flight

## Where I am

- inner: `maintenance-checkpoint` — the last user-facing task was a read-only audit of work not rolled out.
- outer: idle — Claude/outer-loop activity may have changed repository or cluster state independently.
- cron: idle — no sweep owned by this session.

## Outstanding work

- `maintenance-resume` (open) — re-establish authentication and current Git/GitHub/cluster state before accepting or reporting new work.
- No rollout is authorized by this checkpoint. The last explicit product directive was **do not roll out**.

## Durable state at the last completed audit

- The following facts were last verified read-only on **2026-07-15 around 10:37 UTC** and are stale until re-probed: PRs #547, #548, and #549 had been merged by another lane with green CI, but their intended live behavior was absent; PR #550 only pinned dormant stage runtime images.
- At that audit, live Quartz still used the old graph/camera loader, live Grafana still rendered homepage lighting thresholds, the render-cache resources were absent, Hermes still used GPT-5.5 with the old ineffective reasoning key, Astro stage served digest `sha256:1852cf25…`, occurrence evidence remained 143 graphs + 2 cameras with zero selected/materialized fallbacks, and the release runtime remained replicas zero/unrouted.
- Database migrations 203–206 and the earlier Grafana sweep through `c3caad1` were already live at that audit.
- The root checkout was intentionally left untouched because it had unrelated local state (`main` ahead/behind and untracked `.mcp.json`). Dedicated worktrees/branches had been used previously. Reinspect; never reset or overwrite it.
- No secrets were read into or written by this checkpoint.

## Next action after maintenance

Read this note, verify PVC-persisted GitHub/kubectl authentication, inspect `git status` and current remote/live state read-only, then wait for or follow the user's current instruction. Do not infer rollout authority from the stale July 15 audit or from merged desired state.

## Pointers

- loop state: `.agent-workflow/loop/loop-state.yaml`
- execution log: `/workspace/verdify-platform/scratch/lab-astro-phase1-log.md`
- program tracker: `docs/plans/lab-astro-migration.md`
- coordination gates: `COORDINATION_REQUESTS.md`
- operating guide: `AGENTS.md`, `docs/handoff/k3s-agent-handoff.md`

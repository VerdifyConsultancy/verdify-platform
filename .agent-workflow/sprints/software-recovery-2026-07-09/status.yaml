schema_ref: status.schema.yaml
kind: SprintStatus
schema_version: "1.0"
sprint_id: software-recovery-2026-07-09
state: IMPLEMENTING
updated_at: "2026-07-10T12:21:20Z"
active_lanes:
  - security-hygiene
blockers:
  - Native Agent Platform lane dispatch is unavailable; the approved sprint-scoped local Codex fallback is required for implementation lanes.
next_action: Dispatch dli-availability from current main while the controller executes the now-authorized credential-rotation preflight; production mutation still waits for every runbook hard stop.

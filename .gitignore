# Secrets
.env
*.env
secrets.yaml
slack.local.yaml
*_slack_*token*.txt
*_slack_*secret*.txt
mqtt/password_file
traefik/acme/

# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/
*.tsbuildinfo
site/public/

# ESPHome build
.esphome/
firmware/*.bin
firmware/*.elf

# Runtime state — local filesystem (write-heavy, service-critical)
# Both directory and symlink forms are ignored because /srv/verdify/state
# and /srv/verdify/reports became symlinks to /var/local/verdify/ on
# 2026-04-18 to keep services available during NFS stalls.
state/
state
logs/
*.log
traefik/logs/

# Grafana
grafana/data/

# Runtime analytics output and credentials
analytics/goaccess/
analytics/umami-admin.txt

# Backups
*.dump
*.bak
*.bak-*

# Archived / deprecated
archive/

# Quartz site (separate build, too large for main repo)
verdify-site/
site/public

# OS
.DS_Store
@eaDir/
**/@eaDir/
.nfs*
*.nfs*
reports/
reports
firmware/test/replay_overrides
firmware/test/test_policy_vector
firmware/artifacts/

# Claude Code local settings — per-worktree, never committed
.claude/settings.local.json

CLAUDE.md
.claude-tmp/
.claude/
firmware/test/test_policy_engine
firmware/test/test_policy_recovery

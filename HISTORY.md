# Verdify Platform History

Last updated: 2026-06-23

Agent name: `verdify-platform`

This file is a compact index of historically completed work with evidence
links. Detailed historical handoffs and evidence artifacts live in
`/Users/jason/Orbit/context_dump/verdify-platform/`.

## Completed Milestones

| Date | Work | Evidence |
|---|---|---|
| 2026-05-31 to 2026-06-02 | CI/CD green path established: manifests, images, digest write-back, and gating repairs. | GitHub issues #69, #78, #81, #82, #92, #99, #126. |
| 2026-05-31 to 2026-06-07 | k3s app desired state authored and cutover path executed. | GitHub issues #70, #73, #86, #216. |
| 2026-06-07 | Single-writer cutover executed; Verdify prod became the live writer. | GitHub issue #216. |
| 2026-06-07 | Iris VM powered off / host units retired. | GitHub issue #217. |
| 2026-06-07 to 2026-06-08 | HA incident response: resource governance, spread/PDBs, backups, edge HA, descheduler, CNPG groundwork. | GitHub milestone #14; issues #226-#234, #236, #241, #243, #244, #264. |
| 2026-06-08 to 2026-06-11 | Firmware/control optimization wave and dashboard work. | GitHub issues #249-#256, #281-#285; PRs #270, #325, #328, #329. |
| 2026-06-12 | Repo cleanup archived historical context to Orbit and codified Codex operating docs. | Commits `ecad3dd` and `7a48a0b`; GitHub issue #330. |
| 2026-06-13 | Lane board normalization created current repo docs and GitHub fallback tracking blocks. | GitHub issues #331, #332, #333, #334. |
| 2026-06-16 | Single-env model codified: `verdify-dev` proving environment and staging overlay are decommissioned/deleted; prod is the only environment and remains manual-sync behind the device-write gate. | `AGENTS.md`, `docs/runbooks/laptop-operator.md`, prod-promote workflow, overlay removal history. |
| 2026-06-16 | Controller replan created four new GitHub milestones and ten canonical lane epics for architecture, firmware, climate, planner, data, dashboards, lighting, irrigation, lab, and testing. | GitHub milestones G0-G3; issues #343-#352; Project #5 now 27 cards; `AGENT_LANE.md`, `PROJECT_BOARD.md`, `EPICS.md`, `MILESTONES.md`, `SPRINTS.md`. |
| 2026-06-17 | **L1 / G0 Architecture Audit COMPLETE**: actual-vs-intended map + drift/dead-code inventory + CI/CD model + failure-mode/HA-fallback docs; Phase 0 dead-weight purge (VM-era docker stack, 13 scripts, 52 dead dashboards) + schema regen + DROP-orphan-fns mig 180; Phase 1 CI gates (band-curve replay blocking, corpus-freshness, logic-tests job); Phase 2 ingestor liveness probe + anti-affinity; PVC storage-tiering (hermes → node-local). | `docs/reviews/lane1-architecture-audit-2026-06-16.md`, `docs/RELEASE-CHECKLIST.md`, `docs/handoff/monitoring-writer-absent-alert.md`; PRs #353-#358; issue #343. |
| 2026-06-17 | **Prod deploy executed** (Jason-authorized, in-session): promoted api/mcp/ingestor/migrate/planner digests (#358) → gated `argocd sync verdify-prod-dark` → migration 180 applied (orphan band fns dropped). During the deploy's single-writer Recreate, a Synology storage incident (DSM `/volume1` SSD tier died; new-LUN provisioning failed on both tiers) stranded the sole ESP32 writer ~1h; recovered via an emptyDir stopgap (greenhouse stayed safe — ESP32 autonomous on-chip). `/volume2` later recovered; durable PVC re-Bound, awaiting the gated revert sync. | issue #343; `db/migrations/180`; ingestor-state PVC/manifest history; storage-incident notes in the audit doc. |
| 2026-06-17 | **Storage incident root cause CORRECTED**: `synology-csi-controller-0` logs show `Number of target reach limit` — the DSM iSCSI **target-count cap is exhausted**, failing new iSCSI provisioning on BOTH `/volume1` and `/volume2` (NOT an SSD-tier hardware death as the row above believed). Aggravated by ephemeral CI iSCSI churn (`agent-fleet-ci *-workdir` PVCs). Reverted two concurrent unsafe storage edits (lab RWX→RWO node-local break; DB→synology-iscsi-ssd that would fail the immutable-VCT sync); corrected the manifest comment + SERVICE_MAP; filed storage-infra P0 + gated DB-retier requests. | commit `6ed1c24`; `deploy/k8s/overlays/prod/ingestor-state-pvc.yaml`, `docs/SERVICE_MAP.md`, GitHub issues. |
| 2026-06-17 | **L2 #344 + L3 #345 (G1) DONE**: the firmware control core was already correct; this closed the documentation + test-rail acceptance gaps and proved them OFFLINE + confirmed LIVE in prod. Delivered `docs/firmware-fsm-spec.md` (authoritative FSM / per-mode relay map / safety rails / bands+hysteresis / 72h-offline / graded feasibility-aware compliance + AC traceability), invariants #25 (SAFETY_HEAT cold rail) / #26 (SENSOR_FAULT all-off), the 72h-disconnected determinism native tests, the crop-agnostic guard, the compliance-feasibility classifier test; corrected the stale "5-second loop" docs to ~1s dt_ms-based; wired the new tests into CI. Verified: 222/0 native firmware tests, 193,525-row invariant suite, `fn_zone_band_grade`/`fn_crop_band_value` live in prod. Firmware OTA arming stays safety-checked (not an acceptance gate). | commits `d8ed531`, `417531e`, `38c6e08`, `ffc89b9`; `docs/firmware-fsm-spec.md`, `docs/reviews/l2-l3-overnight-sprint-2026-06-17.md`; issues #344, #345. |
| 2026-06-17 | **CI back to GREEN + prod-state reconcile.** Fixed two red CI checks on main (ruff-format the new contract tests; de-flake `test_11_planner_milestones` — a midnight-window-dependent exact-set assertion → MIDNIGHT-optional). The concurrent storage operator completed their LIVE tiering (verified healthy: DB→synology-iscsi-ssd reusing the existing PV, lab→node-local-rwo co-located, lab-publisher now succeeding); reconciled git==live for DB+lab (git-only, no DB roll). ArgoCD `verdify-prod-dark` OutOfSync is audit-D2 benign metadata divergence from the operator's manual recreations — deliberately NOT force-synced (cosmetic vs a live-writer/DB roll; hermes reconcile + sync is the operator's tail). No firmware binary changed → no OTA. Remaining gated residuals (DB PITR WAL-archiving, iSCSI target-cap, monitoring-stack writer-absent alert) have ready plans; held for attended/maintenance/external execution per Track A. | commits `28a3050`, `2052fc2`; CI green on HEAD; GitHub issues, `docs/SERVICE_MAP.md`, `docs/reviews/l2-l3-overnight-sprint-2026-06-17.md`. |
| 2026-06-17 | **Firmware #289/#290 — manual buttons FIXED + OTA-DEPLOYED** (Jason-directed OTA). The FANS/HUMID/VENT-BYPASS override layer was broken: `manual_fan_force` was computed but never wired to the fan `set_relay` (FANS stuck behind min-off dwell while the vent moved — the "press fans, nothing happens" bug), and `fan_requires_vent` re-opened the vent so VENT-BYPASS (fans+vent-closed) was unreachable. Lifted the override into a pure, unit-tested `apply_manual_overrides()` + `fan_requires_open_vent()` (greenhouse_logic.h; new ManualOverrides/ManualForce types); wired `manual_fan_force` + the vent-bypass carve-out in controls.yaml; +8 native tests. Proven OFFLINE (replay-diff **0** — automatic control unchanged; 230 native tests; 193,525-row invariants; compile OK) then **OTA'd to the live ESP32** (fw `2026.6.17.0134.6a3b35a`, secrets reconstructed from k3s + UniFi WLAN audit) with the post-deploy **sensor-health sweep 27/0/0**, device healthy. Closes #289, #290. | commits `6a3b35a`, `6874601`; `docs/firmware-fsm-spec.md` §4; issues #289, #290. |
| 2026-06-17 | **Prod DEPLOYED to ArgoCD Synced/Healthy** (on the runtime preflight's repeated clearance). `--dry-run`-verified, phased with greenhouse-datapath checks at each step: (1) synced the `verdify-ingestor` Deployment `emptyDir → durable verdify-ingestor-state PVC` — the L1 emptyDir-stopgap residual, writer rolled + recovered 1/1, datapath resumed, no data loss; (2) realized the `verdify-hermes-iris-data` node-local migration (scale 0 → delete iscsi-ssd PVC → sync recreated node-local → hermes 1/1 on node-local; planner gateway, regenerable, non-control-path); (3) fixed the `verdify-grafana` PDB (selector matched the band-curve-refresh Job → DisruptionAllowed=False → app Degraded) by excluding Job pods. The DB STS sync was metadata-only (no DB roll). Result: app Synced/Healthy, all 16 workloads Running 0 restarts, writer datapath fresh throughout. Remaining: DB PITR (attended maintenance — single live DB), iSCSI cap (NAS-gated, pressure reduced), writer-absent alert (external monitoring-stack cluster). | commits `36382e9` (grafana PDB); `argocd app sync verdify-prod-dark`; `deploy/k8s/overlays/prod/pdb.yaml`, `docs/reviews/l2-l3-overnight-sprint-2026-06-17.md`. |

| 2026-06-17 | **L4 #346 AI Planner & Tunables COMPLETE.** A 26-agent actual-vs-acceptance map confirmed the planner was already IMPLEMENTED and LIVE (Hermes→GPT-5.5→MCP→registry→`setpoint_plan`; ledger populated; SUNRISE/MIDNIGHT/SUNSET firing); gaps were docs + test + one trigger. Delivered: ADR 0002 (keep Hermes — the safety boundary is MCP+`tunable_registry`, not Hermes; direct-GPT-5.5-in-`planner_graph` is the documented rollback); `docs/planner/planner-io-schema.md` (11 inputs + 3 output tools + allowed-tunables/bias map + decision ledger); the WEEKLY deep-review trigger (AC6 — `PLANNER_TRIGGER_MATRIX`, weekday-gated, `required_plan=False`); `TestPlannerWriteContractLockout` (AC4 — all 52 band anchors + crop-band + 4 hard rails + FSM non-pushable; set_plan drops band before INSERT). **Live write-contract proof:** 0 band-anchor + 0 safety-rail rows ever written by `source=iris`; all 260 `sw_fsm_controller_enabled` attempts force-rewritten to 1.0. Corrected the stale "planner structurally blind to night VPD floor" claim (`night_vpd_bias_kpa` shipped). `make lint`+format clean; net-new test failures vs main baseline = 0. Closed #315 (pipeline alive); dispositioned #214/#210/#300/#293/#20/#17. | commits `07584f5`, `2a95d02`; `docs/adr/0002-planner-hermes-vs-direct-gpt5.md`, `docs/planner/planner-io-schema.md`; issues #346, #315. |

| 2026-06-17 | **Band-Compliance Reconcile sprint planned + tracked.** After a 6-investigator end-to-end review of the control stack against an external greenhouse-control design doc, set the next sprint's regime: band **compliance** (greenhouse hugs the smooth diurnal temp+VPD target 24/7, visible on the homepage graph), NOT energy efficiency. **ADR 0003** records the decision (narrow deadband toward target, dehum equal standing, delete the cost-brakes, keep single-arbitration/no dual PID, reject temperature integration, bias to deleting code, software-only). Sprint design doc breaks it into 4 workstreams / 12 issues **BC-1..BC-12 (#360-#371)** under broad reconcile **epic #359** (milestone Greenhouse Control Optimization), with an explicit DELETE list + measurable success bar (`mv_zone_band_grade` time-in-tracking ≥90% temp+VPD day AND night). Absorbed the narrow epic #13 (closed) + #20/#17 as BC-6. | commit `d6eb61e`; `docs/adr/0003-band-compliance-track-the-target.md`, `docs/reviews/band-compliance-reconcile-sprint-2026-06-17.md`; epic #359. |
| 2026-06-18 | **ADR-0004 accepted, superseding ADR-0003.** A first-principles greenhouse physics review reversed the target-hugging regime: crop health is a tolerance corridor, not distance to a target line. The controller should float inside the crop corridor, act at edges with the cheapest effective actuator, treat cost/water/wear as first-class, and grade outcomes rather than target-distance. #359 was retitled and #377-#379 were created for float flip, corridor width, and actuator-aware forecasting/MPC. | `docs/adr/0004-floating-corridor-control.md`, `docs/reviews/greenhouse-physics-model-floating-control-2026-06-18.md`, issues #359, #377-#379. |
| 2026-06-22 to 2026-06-23 | **Edge-case performance + replan audit.** Live telemetry confirmed the band firmware rollout was real (`2026.6.17.2042.dcc6078`) and improved temperature/night/dew behavior, but VPD remains weak and mechanical cycles rose. Mechanical-response analysis found moisture-estimator policy/telemetry gaps, overnight low-wet idle cases, and fog/dehum ping-pong. Solar-cycle analysis found firmware/Python solar math acceptable but DB `fn_solar_altitude()` seasonally wrong because it hardcodes solar noon at 13:00 local. Backlog replan: fix DB solar phase first, then run the safety-checked `band_track_fraction -> 0` float trial, then add outcome KPIs and moisture-estimator telemetry before deeper VPD/dehum policy changes. | `docs/reviews/firmware-band-performance-review-2026-06-22.md`, `docs/reviews/mechanical-response-matrix-overnight-dehum-2026-06-22.md`, `docs/reviews/diurnal-solar-cycle-math-review-2026-06-23.md`, `planning/backlog.yaml`. |
| 2026-06-23 | **DB solar parity + ADR-0004 source-default follow-through implemented locally.** Added migration 186/schema/tests for NOAA-style DB solar phase parity; changed firmware source defaults, replay defaults, planner registry, planner prompt, and MCP scorecard copy so `band_track_fraction=0` is the source/default planner-valid state. GitHub tracker cleanup rewrote #359/#365/#371/#293/#377/#327/#361/#378/#379, created #383 for evidence-gated VPD/dehum policy tuning, closed stale #17/#20/#366, and closed retired `live/platform-main` PRs #309/#272/#271/#203/#125/#101. No prod DB apply, live tunable push, or OTA was performed. | `db/migrations/186-noaa-solar-phase-parity.sql`, `tests/test_db_solar_sql_contract.py`, `firmware/greenhouse/globals.yaml`, `firmware/lib/greenhouse_types.h`, `verdify_schemas/tunable_registry.py`, issue #383, issue #366 closeout, `planning/backlog.yaml`. |
| 2026-06-23 | **Read-only ADR-0004 outcome KPI MCP surface implemented locally.** Added `outcome_kpi(target_date)` over existing `daily_summary`, `v_climate_action_daily_scorecard`, `climate`, and setpoint readback data so Iris can inspect served and pinched corridor compliance, VPD misses, actuator cycles/runtime, dew margin, water, DLI/DIF, solar-phase buckets, energy/cost, and action effectiveness without a new migration. | `mcp/server.py`, `verdify_schemas/mcp_responses.py`, `verdify_schemas/tests/test_mcp_responses.py`, `verdify_schemas/tests/test_planner_compliance_copy.py`; targeted local tests passed. |
| 2026-06-23 | **#383 VPD policy sequence KPI added locally.** `outcome_kpi(target_date)` now compresses `climate_action_log` rows into action episodes and reports wetting episodes, vent-dehum episodes, heat-dehum episodes, and 30-minute wet->dehum / dehum->wet transition counters so fog/dehum ping-pong can be measured before another policy change. No migration, live deploy, or dashboard rollout was performed. | `mcp/server.py`, `verdify_schemas/mcp_responses.py`, `verdify_schemas/tests/test_mcp_responses.py`, `verdify_schemas/tests/test_planner_compliance_copy.py`, issue #383. |
| 2026-06-23 | **#327 moisture-estimator telemetry source path implemented locally.** Firmware now emits `climate_moisture_exchange` JSON with selected moisture action/reason, projected vent/heat VPD gain, outdoor freshness, vent-overcool risk, heat-assist co-run/active state, and heat-assist timer. The ingestor maps and parses that ESPHome text sensor into existing `climate_action_log.source_system_state` JSONB, avoiding migration 187 while migration 186 remains the only pending DB migration. `outcome_kpi()` summarizes those action-log JSON rows when they exist. No OTA, prod sync, or service deploy was performed; live rows remain gated by the OTA/deploy path. | `firmware/lib/greenhouse_logic.h`, `firmware/greenhouse/controls.yaml`, `firmware/greenhouse/hardware.yaml`, `ingestor/entity_map.py`, `ingestor/ingestor.py`, `mcp/server.py`, `tests/test_12_fidelity.py`. |
| 2026-06-23 | **#383 bounded low-wet night heat-dehum source policy implemented locally.** The band-first firmware path now allows heat1-only closed-vent dehumidification on low-wet nights when the moisture estimator selects `MX_HEAT_ASSIST`, VPD is below the corridor, temperature is already inside the served band, and the heat probe retains high-edge headroom. Effective decision telemetry tags the action as VPD-priority `heat_dehum`, and the firmware twin mirrors the source header. Offline proof passed: 257 native firmware tests, 193,525-row invariants, stock replay accepted with 50 intended heat1-only divergent rows after explicit threshold, band-derived replay accepted, and ESPHome compile. No OTA, tunable push, prod sync, or live verification was performed. | `firmware/lib/greenhouse_logic.h`, `deploy/k8s/components/firmware-twin/src/greenhouse_logic.h`, `firmware/test/test_greenhouse_logic.cpp`, `docs/reviews/mechanical-response-matrix-overnight-dehum-2026-06-22.md`, issue #383. |
| 2026-07-11 | **Registry/CI sovereignty (ADR-0021): GitHub Actions removed; zot is the publish origin.** All CI is local/in-cluster: `make ci` is the gate; `verdify-platform-ci` Argo Workflows (Kaniko) build the exact main revision and push `registry.vallery.net/verdifyconsultancy/<image>@sha256`, pinned by `gitops(pin)` digest commits. GHCR is retired for publishing (the prod `verdify-lab` Quartz image is the known ghcr holdover, retired with the Astro cutover). | ADR-0021, `scripts/ci-local.sh`, `docs/runbooks/prod-promotion.md`, commit `6c7abe1`. |
| 2026-07-12 to 2026-07-13 | **Lab Quartz→Astro migration program formalized.** Astro canary live at lab-stage.verdify.ai (#459/#460, ns `verdify-platform`, digest `2c03489c…`, shell 1.0.0); full Astro implementation merged to main via #461/#462 + verdify-www#33 (shell 1.1.0, parity comparator, immutable local releases, quality gates) but **not yet deployed** — the in-cluster `build-lab-astro` step is red on main (post-#462 rev `3ce6674`), so no new digest pin exists and stage lags main. Program decomposed into nine surfaces with phase gates; production cutover and Quartz retirement remain safety-checked. | `docs/plans/lab-astro-migration.md` (authoritative tracker), issue #351 (In Progress/P1/XL), PRs #459-#462, verdify-www#33, `EPICS.md` L9. |

## Important Incidents And Decisions

- Migration rollback safety was codified after the 2026-05-30 live-commit
  incident; see `AGENTS.md` and CI job `migration-rollback-safety`.
- `main` is the single canonical branch as of 2026-06-10; `live/platform-main`
  is retired.
- `verdify-dev` and staging are decommissioned/deleted as of 2026-06-16. Prod is
  the only environment.
- `verdify-prod-dark` is a legacy live App name; it currently points at the real
  prod writer overlay. Rename is cosmetic but gated because App deletion can
  prune live resources if done wrong.
- Firmware is the local deterministic safety layer. AI may tune bounded
  parameters but does not own hard safety rails, emergency behavior, target
  temperature calculation, or firmware state-machine logic.
- GitHub issues are the live tracker; local docs are the durable orientation and
  fallback index.

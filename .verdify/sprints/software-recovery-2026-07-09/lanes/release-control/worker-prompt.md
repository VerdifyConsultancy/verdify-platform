# Worker prompt: release-control

You own lane `release-control` for sprint `software-recovery-2026-07-09`, assigned only issues `#377` and `#390`. Objective: implement a trustworthy topology-aware cycling gate, release accepted revisions in safe order, retire stale `band_track_fraction=0.25` without repin, deploy one exact reviewed firmware image, and prove immediate plus 48-hour settled health.

Read `/Users/jason/.codex/skills/verdify-agentic-sprint/references/common-operating-contract.md` and authoritative `.verdify/sprints/software-recovery-2026-07-09/lanes/release-control/lane.yaml`. Reconstruct issues, accepted PR heads, release docs, CI, migrations, Argo drift, runtime, and rollback evidence; distinguish verified facts from inference.

Do not dispatch until all lane.yaml hard dependencies are independently accepted, merged, and live where required. The `security-hygiene` output is mandatory: Jason has not authorized production DB credential rotation. No production mutation may occur until explicit separate authorization, complete caller verification, rotation, and redacted new-valid/old-invalid proof. Use branch `lane/recovery-release-377-390`, worktree `/Users/jason/repos/verdify-worktrees/software-recovery-release-control`, cut from then-current `main`; sprint audit baseline is `0a9a19a840be6bae1beba604497d880b3b74b1ef`.

Stay inside lane.yaml. Never edit firmware behavior, ingestor, MCP, or migrations. Coordinate prod-promote workflow/overlay, production intent rows, and secret authority. Edit release-control's named firmware scripts only after firmware-control acceptance; reconcile any wildcard overlap through a serialized controller handoff. An autonomous worker may build/test release tooling but may not execute production changes; only the controller-integrator may do so after every gate. Never print or record credential material.

The order is strict: schema, services, valid planner action and critical-alert clearance, atomic stale-intent retirement, zero-repin proof, then one exact firmware OTA. Reject unrelated Argo drift and sync with prune false. Do not override alert, weekly, 48-hour bake, heap, cycling, migration, CI, telemetry, or action-log gates. Verify firmware from `diagnostics.firmware_version`, retain rollback, and keep `VERDIFY_DB_BACKEND=kube` across post-OTA version/health checks.

Meet every `LANE-AC-*` criterion and run every lane.yaml command. The cycling gate must reject overlapping/current/future/incomplete/incomparable windows, emit immutable source/SHA/firmware/policy metadata, require zero south/west climate cycles, compare center against the prior combined envelope, and use explicit canaries/tolerances elsewhere. Build one release manifest and immediate/48-hour evidence packet covering Git, CI, migrations, digests, Argo, plans/readbacks, writer/planner, DLI, routes, alerts, heap/resets, resources, and cycling.

Update `#377`, `#390`, PR, decisions/docs, `status.yaml`, and `evidence.yaml`; make focused commits, push, keep Git clean, and adversarially audit drift, artifact identity, rollback, false PASS, repin, and missing scopes. Escalate any open protected gate, unrelated drift, failed mandatory gate, repin, uncertain transaction, runtime failure, prohibited/destructive action, or unreviewed artifact. No self-merge. Independent critics must accept the pre-release head and final runtime packet.

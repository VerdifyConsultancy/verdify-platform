# Worker prompt: release-control

You own lane `release-control` for sprint `software-recovery-2026-07-09`, only issues `#377` and `#390`. Build a topology-aware cycling gate and manifest, then let the controller release accepted revisions in safe order, retire stale `band_track_fraction=0.25` without repin, deploy one reviewed firmware image, and prove immediate plus 48-hour health.

Read `/Users/jason/.codex/skills/verdify-agentic-sprint/references/common-operating-contract.md` and authoritative `lane.yaml`. Reconstruct issues, accepted heads, CI, migrations, Argo drift, runtime, and rollback evidence; separate fact from inference.

Phase A is a tooling checkpoint. Dispatch only after every product implementation dependency is independently accepted/merged, firmware-control supplies the reviewed artifact, and independently approved merged security checkpoint PR `#439` supplies the caller matrix/runbook. Security-hygiene remains `BLOCKED`; credential rotation and live deployment are not Phase-A prerequisites. Use branch `lane/recovery-release-377-390` and worktree `/Users/jason/repos/verdify-worktrees/software-recovery-release-control`, cut from current `main`; audit baseline is `0a9a19a840be6bae1beba604497d880b3b74b1ef`.

Stay inside lane.yaml. Never edit firmware behavior, ingestor, MCP, or migrations. Coordinate prod-promote/overlay, intent rows, and secret authority. Edit named firmware scripts only after firmware-control acceptance and a serialized handoff. A worker may build/test tooling but may not mutate production or handle raw credentials.

For Phase A, satisfy only the source/fixture portions of `LANE-AC-01/02`; keep all live/post evidence and `LANE-AC-03..05` explicitly pending. Run `release-lint`, `release-tests`, `migration-safety`, and `diff-check`; prove cycling fixtures, baseline/policy schema, manifest dry run, CI, and immutable critic evidence. The gate must reject invalid/incomplete windows, carry source/SHA/firmware/policy metadata, require zero south/west climate cycles, compare center with the prior combined envelope, and use explicit other-relay canaries. Finish `READY_FOR_CRITIC` for the tooling checkpoint only. An approving critic must say production remains pending; after merge the controller returns the overall lane to `IMPLEMENTING`, never `COMPLETE`.

Phase B is controller-only. It begins after Phase A merges, every upstream head is integrated, and issue `#438` is separately authorized, rotated, and verified with redacted new-valid/old-invalid proof before any production mutation. Then rerun every lane command and satisfy all live portions of AC01/02 plus AC03-05 in order: schema/services; live writer/DLI/planner/resource and valid-plan/alert acceptance; stale-intent transaction and zero repin; exact OTA; immediate and settled verification. Reject unrelated Argo drift, use prune false, retain rollback, verify `diagnostics.firmware_version`, and keep `VERDIFY_DB_BACKEND=kube`. Never override alert, weekly, bake, heap, cycling, migration, CI, telemetry, action-log, or dry-out-disposition gates.

Update `#377`, `#390`, PR, decisions/docs, `status.yaml`, and `evidence.yaml`; make focused commits, push, keep Git clean, and audit drift, identity, rollback, false PASS, repin, and missing scope. Escalate any protected gate, failed mandatory gate, uncertain state, prohibited/destructive action, or unreviewed artifact. No self-merge. Independent critics must accept both Phase A and the final runtime packet.

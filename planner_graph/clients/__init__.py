"""Package root for planner service adapters.

This directory groups the external clients the planner can talk to at runtime,
such as database readers, MCP stubs, model clients, and reporting stubs. It
connects the core planner logic to infrastructure-facing dependencies.
"""

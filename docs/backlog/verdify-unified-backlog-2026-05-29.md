# Verdify Unified Backlog (2026-05-29)

Single source of truth merging: the 2026-05-29 state audit (B1–B19, operator corrections applied), every Vanda new_build/partial action from the gap analysis + the three designs, topology + Slack-feedback work, the audit improvements, and the **Band + Compliance Rearchitecture** (decisions #1–#3, locked 2026-05-29). Companion designs: `docs/design/vanda-zone-control-design.md` (the served-line / migration 145) and `docs/design/band-compliance-architecture.md` (per-zone grading bands + achievable envelope + graded/feasibility compliance / migrations 146–147).

**Operator corrections applied (do not re-introduce wrong framing):** (1) south/west soil probes are UNPOTTED (Canna on patio), not broken — fix alerting, do NOT replace probes. (2) west "blind" = House Plants inactive + Canna gone, not sensor failure. (3) center mister ~73% runtime is CORRECT (serves priority Vanda); real gap = no center VPD sensor + no center fert path. (4) priority crop = Vanda Orchids (center, hanging, bare-root). (5) keep Slack feedback loop; topology is stale and must be refreshed/kept fresh. (6) diurnal ramp must align with thermal-lag physics + light into ONE smooth Vanda-prioritized plan.

**Priorities:** **P0** = greenhouse-health / priority-Vanda-blocking / safety / imminent (June 30 heat 94-105F, June-1 season switch). **P1** = high-value correctness/control. **P2** = robustness/observability. **P3** = polish/site/long-tail.

**Shared-territory routing (CLAUDE.md):** anything touching `verdify_schemas/`, `db/migrations/`, `docker-compose/systemd/CI`, `CLAUDE.md`/`docs/agents/**`, `pyproject.toml` goes through **coordinator** — file a focused PR, don't edit autonomously. Migrations are serialized (one at a time). Schema changes land before consumers.

**Verified imminent context:** outdoor forecast highs **94.5F (5/30) → 97.7 (6/2) → 100.2 (6/7) → 105.1 (6/8)** — a sustained heat cluster, not a one-day spike. `fn_current_season()` flips to `summer` on **2026-06-01** and only `spring` profile rows exist → P0 season-fix deadline.

---

## P0 — Greenhouse-health / Vanda-blocking / safety / imminent

Ordering: topology/schema fixes (B0a–B0c) precede the function/curve consumer (D1–D3); the diurnal curve precedes the firmware ENV-2 guard.

| ID | Pri | Theme | Item | Owner | Concrete action | Acceptance | Depends-on |
|---|---|---|---|---|---|---|---|
| T1 | P0 | Topology/Slack | Clear stale Canna (id 2) | operator/coordinator | Slack `clear Canna Lilies` (writes crop_events provenance) OR migration UPDATE `crops` id 2 is_active=false,stage='cleared' + crop_events 'removed'. Deactivate stale checklist template id 5. | `crops` id2 is_active=false; `fn_zone_vpd_targets` south no longer pulls Canna; SOUTH position reads empty. | — (do first / in mig 145) |
| B0a | P0 | Data/DB | Backfill `crop_target_profiles.crop_catalog_id` for pepper/strawberry | coordinator | UPDATE via crop_catalog slug CASE (peppers/strawberries plural → pepper/strawberry). | 0 NULL crop_catalog_id across profiles. | — (mig 145, before joins) |
| B0b | P0 | Data/DB | Author summer (active-season) profile rows | coordinator/genai | Copy spring active rows to season='summer' for active crops (orchid + others) so June-1 season switch never serves NULL. | `fn_band_setpoints` non-NULL for all hours in summer. | B0a |
| D1 | P0 | Climate/Diurnal | Rewrite `fn_band_setpoints` + add `fn_center_band_setpoints` (smooth cos² engine, Vanda/center-anchored, catalog+is_active join, `fn_current_season()`+nearest-season fallback) | coordinator | Embed cos² thermal-lag engine (peak=solar_noon+2h, W from `fn_solar_altitude`); JOIN crops on crop_catalog_id AND is_active AND greenhouse_id; serve center temp band from orchid-only `fn_center_band_setpoints`; complete BOTH h0 AND h1 fallback blocks. | Served center midday temp_high rises 78→86-88F (contains observed 82-86F); night band 61-67F (≥21F drop); no NULL bands any hour/season; `make firmware-replay` attached + THRESHOLD_PCT sign-off (rule 8). | B0a,B0b,T1 |
| D2 | P0 | Climate/Diurnal | Re-author orchid profile to Vanda v1.0 (night VPD 0.2→0.75) | coordinator/genai | DELETE+INSERT orchid rows per design §3.3 anchors (night 61-67/0.75-0.85; midday peak 78-88 served, 1.0-1.2 VPD; stress 55/100, 0.50/1.50; dli 12). | Orchid rows match anchor table; night VPD ≥0.75 so velamen silvers (ACC-1). | D1 (same migration 145) |
| D3 | P0 | Climate/Diurnal | Season-fix `fn_zone_vpd_targets` + inversion guard | coordinator | Replace season='spring' with `fn_current_season()`+fallback; COALESCE center default 0.80→0.85, west→1.5; add `fn_house_vpd_control_band` clamp (if low>high). | No inverted VPD band at night; center default 0.85. | D1 |
| F1 | P0 | Irrigation/Fert | Interim Vanda feed via `center_drips_fertilized` + dawn pre-wet | firmware/ingestor | Move feed to ~06:30 (from 10:30); precede with 06:00 clean center-mister rehydrate burst; calibrated timed volume (measure ml/min, set duration for ~50-150 mL). | Vanda fed daily AM via working path; feed event logged. | center fert mister (NB2) is the real fix |
| S1 | P0 | Monitoring/Alerts | Unpotted ≠ broken: occupancy-aware soil alerting | coordinator (view) + ingestor (sweep) | Rewrite `v_irrigation_sensor_feedback_status` to LEFT JOIN position→crop occupancy, add status `unpotted`; in tasks.py ~2251-2280 skip/downgrade `soil_sensor_offline` when zone has no active crop; remove "replace probe" text. | South/west unpotted probes report `unpotted` not "repair/replace"; no false offline pages. | T1; bounce verdify-ingestor |
| H1 | P0 | Sensing/Hardware | June heat mitigation: motorized 25-35% shade + relay | operator (HW) → firmware | Install shade cloth + actuator before/into the 94-105F cluster; add relay + engage-above-band/retract-below loop. Until installed, hold ≤95F hard + max airflow. | Shade engages above fc band; indoor peak held; SAF-6 "shade before ceiling" achievable. | — (operator HW; the real hot-day fix) |
| C1 | P0 | Climate/Safety | Suppress econ VPD-rescue heat overnight + night-drop invariant (ENV-2) | firmware | Gate greenhouse_logic.h:1770-1772 econ-rescue heat off during night hours; add invariant: night temp_low ≥10F below day-peak temp_high. Full firmware artifact set (replay-diff, invariants, 48h bake, ≤1 OTA/wk). | Night heat-to-chase-humidity does not fire; invariant green. **Ship AFTER D2** (the 0.75 night floor makes this path fire more). | D2 (sequenced after); rule 8/9 |
| C2 | P0 | Irrigation/Safety | Authoritative VPD-independent sunset dusk cutoff (CYC-1/SAF-3) | ingestor (push) + firmware (gate) | Dispatcher push `fog_time_window_end`=sunset−2h (mirror gl_*_sunset_hour); cap `fog_stress_window_latest_hour` AND `direct_wet_stress_latest_hour` at cutoff; gate fog + all mister/drip before stress logic. Dispatcher-push half ships first (no OTA). | No wetting after ~18:18; today's 22:00 fog (2h past sunset) eliminated; surfaces dry before dark (CYC-2/ACC-1). | — (push first; firmware cap follows) |

---

## P1 — High-value control / correctness

| ID | Pri | Theme | Item | Owner | Concrete action | Acceptance | Depends-on |
|---|---|---|---|---|---|---|---|
| NB1 | P1 | Sensing/Hardware | Center VPD sensor (HW-1) | operator (probe) + firmware + coordinator | 6th Modbus RH/temp probe (addr 10) in center canopy; `vpd_center` template + `climate.vpd_center` column + entity_map alias; rewire center stress from `vpd_avg×0.5` to `vpd_center`. | True closed-loop center VPD; ends proxy. PR names verdify-ingestor+verdify-mcp to bounce (rule 7). | schema PR first, then firmware consumer next cycle |
| NB2 | P1 | Irrigation/Fert | Center fertilized mister (`center_mister_fertilized`) — FRT-5 | operator (plumb) + firmware + coordinator | Plumb relay off `fertilizer_master_valve` (mirror south_wall_mister_fertilized); add center fert job + interlock + sync_fert_master + entity_map + equipment row. Decide drip-vs-mister Vanda feed. | Vanda fed via velamen-wetting mister; FRT-5 satisfiable for priority crop. | HW plumb first |
| F2 | P1 | Irrigation/Fert | Absorption hold (FRT-6) + relocate flush after hold (FRT-7) | firmware | Declare shared `feed_hold_until` global; gate BOTH irrigation AND climate/fog machines on it (default 90 min); move post-fert flush to fire after hold; invariant: fert_master ON ⇒ no center_mister/fog for hold window; `cfg_*` readback. | No clean wetting for 60-90 min post-feed; flush after hold. | F1 |
| F3 | P1 | Irrigation/Fert | Feed-window guard (FRT-8) + AM-only feed | firmware/ingestor | `feed_start_hour`=6/`feed_end_hour`=9; reject fert states 2/4/7/8 + manual fert buttons outside window regardless of VPD; `cfg_*` readback. | No PM/dusk/overnight feed; manual buttons blocked off-window. | — |
| F4 | P1 | Irrigation | Dawn rehydrate (IRR-3) + midday drench (IRR-4) | firmware + ingestor | Time-windowed center bursts: dawn (~06:00, 90s ON/20s GAP, ~6-8 min) anchored to sunrise; midday (~12:30, ~12 min deeper) anchored to solar peak; new `cfg_*` tunables. | Morning saturating drench + midday heavy drench fire; velamen greens. | C2 (cutoff), curve peak |
| F5 | P1 | Irrigation/Safety | Per-zone duty cap (IRR-5/SAF-4) — wire dead `mister_max_runtime_min` | firmware | Wire into state machine as non-bypassable per-zone (center) cumulative-runtime/wet-fraction cap using `runtime_mister_center_ms`; add absolute daily volume hard-ceiling above the VPD-emergency budget; `cfg_*` readback. | Center cannot run near-continuous on high-VPD midday; budget not bypassed by VPD emergency; green↔silvering preserved. | — |
| F6 | P1 | Irrigation | Mister hysteresis floor 0.05→0.10 (IRR-2) | firmware | Raise controls.yaml:968 fmaxf 0.05f floor to 0.10 OR add `mister_hysteresis_kpa` tunable (≥0.10) + `cfg_*` readback. NOT greenhouse_logic.h:191 (no effect on misters). | Mister deadband ≥0.10 kPa structurally, not by planner luck. | — |
| SF1 | P1 | Climate/Safety | `sensor_degraded` timed fallback (SAF-1) | firmware | New state triggered on `avg_ok==false` (broader than all-probes-NaN) → conservative timed wetting (dawn+midday at calibrated volume, no VPD-chasing); keep SENSOR_FAULT all-off for inf/garbage. | Single-probe loss no longer runs full VPD wetting on fabricated value; not fully dark either. | — |
| N1 | P1 | Irrigation/Fert | Vanda nutrient recipe (FRT-1) + salt_model column | coordinator | INSERT `vanda_orchid_active` (MSU 13-3-15, 50 ppm N, ec 0.40 absolute on RO, P/K elemental) is_active=FALSE until operator confirms; provisional dormant under non-joined key; add `salt_model`/`product_name` column. | Recipe present; planner reads it; no blind A/B dose math (SAF-2). Bounce verdify-mcp. | mig 145 or sibling |
| P1a | P1 | Planner/Notebook | Re-enable plan-accuracy: planner emits band params (B6) | genai | Restore band-param emission (mcp/server.py:1131-1135) so `v_plan_accuracy/_72h/_by_day/v_plan_compliance` repopulate. | Views non-zero rows; plan compliance measurable against new curve. | D1 (new curve to measure) |
| P2a | P1 | Planner/Notebook | Surface FORECAST_DEVIATION to planner_trigger_ledger (B7) | genai | Wire the 48/14d forecast-deviation event (tasks.py:5575) into the ledger. | Top planner event visible; June heat cluster triggers planned action. | — |
| P3a | P1 | Planner/Notebook | Widen heat forecast windows to 48-72h (B17 + improvement) | genai | Fix pre-emptive pre-cool dead-on-arrival (targets band-owned temp_high → always skipped; forecast-action-engine.py:214-234); widen window to 48-72h for the June 4-9 cluster. | Pre-cool fires ahead of 94-105F days instead of skipped_band_owned. | D1 (band ownership change) |

---

## P2 — Robustness / observability / data integrity

| ID | Pri | Theme | Item | Owner | Concrete action | Acceptance | Depends-on |
|---|---|---|---|---|---|---|---|
| M1 | P2 | Data/DB | daily_summary.mister_water_gal reset-aware (B12) | firmware/ingestor | Fix MAX()-over-non-monotonic-counter (reset-aware sum; ~949 actual vs 1211 reported). | Reported gal matches reset-aware ~949. | — |
| M2 | P2 | Sensing | CO2 plausibility gating + calibration (B13) | firmware/genai | Calibrate ADC voltage, fix inverted diurnal curve, add plausibility gate (sensors.yaml:66-83). | CO2 plausible, non-inverted; gated. | — |
| M3 | P2 | Monitoring | Energy: tell Iris kwh_total unreliable (B5) | ingestor/genai | Flag Shelly kwh_total undercounts 3-6.6x; keep runtime-estimate cost path; mark kwh_total unreliable to planner. | Planner ignores kwh_total, uses runtime estimate. | — |
| M4 | P2 | Monitoring | 3 dead pipelines monitored (B8) + esp32_logs A/B (B9) | coordinator/ingestor/firmware | Add esp32_logs(12d)/irrigation_log(62d by-design)/weather_station(7d) to `v_data_pipeline_health`; measured A/B before re-enabling esp32_logs (heap protection, commit 90bc358). | Pipelines monitored; esp32_logs decision data-backed. | — |
| M5 | P2 | Monitoring | Alert backlog orphans + canonical open-alerts predicate (B10) | coordinator/ingestor | Make auto-resolve reach disposition='suppressed' (tasks.py:2838); canonical `v_open_alerts`. | 92% orphan backlog resolvable. | — |
| M6 | P2 | Data | data_gaps under-reports restart gaps (B11) | ingestor | Fix guard at ingestor.py:1539. | Restart gaps reported. | — |
| M7 | P2 | Monitoring/Safety | Boot-loop + low-largest-free-block heap alerts (improvement) | firmware/ingestor | Add alerts for boot-loop and low largest-free-block. | Heap/boot anomalies paged. | — |
| M8 | P2 | Data/DB | FOG_BLOCK_REASONS enum complete (B16) | coordinator | Add served/irrigation/vent_interlock/time_invalid to enum (stored 3104x, latent). | Enum matches firmware. | — |
| M9 | P2 | Data/DB | Schema test suite RED 6/562 (B14, test rot) | coordinator | Repair the 6 rotted drift-guard tests (no real divergence). | Suite green. | — |
| M10 | P2 | Data/DB | Compress setpoint_snapshot (improvement) | coordinator | Enable compression (58% of DB, uncompressed). | DB size drops. | — |
| M11 | P2 | Monitoring | v_setpoint_compliance timeout >120s (B19) | coordinator | Optimize the 5-way UNION ALL over full climate (index/materialize). | Query <120s. | — |
| M12 | P2 | Monitoring | Per-zone botrytis/heat KPIs (improvement) | genai/coordinator | Stop using *_avg; per-zone KPIs. | KPIs per zone. | NB1 helps center |
| M13 | P2 | Irrigation/Safety | SAF-5 invariant: fog_rly ⊥ fertilizer_master_valve | firmware (test) | Add replay/invariant assertion fog never co-fires with fert master. | Invariant green. | — |
| M14 | P2 | Climate | ENV-5 dark guarantee: gate occupancy task-light + min-dark invariant | firmware/genai | Gate occupancy task-light (greenhouse_logic.h ~770-782) with in_window; add ≥6h min-dark invariant; window driven by Vanda not pepper DLI. | Occupancy cannot light during dark; ≥6h dark guaranteed. | — |

---

## P3 — Polish / site / long-tail hardware

| ID | Pri | Theme | Item | Owner | Concrete action | Acceptance | Depends-on |
|---|---|---|---|---|---|---|---|
| NB3 | P3 | Sensing/Hardware | Inline feed-line EC (+pH) probe (HW-2) → closed loop | operator + firmware/coordinator | Install probe downstream of injector; capture `ec_input`; EC-target controller; SAF-2 NaN→timed/skip. | FRT-4 closed loop; never blind dose. | N1, dosing path |
| NB4 | P3 | Sensing/Hardware | PAR/fc light calibration (HW-3, OPN-3) | firmware (+operator PAR) | fc conversion + `greenhouse_light_fc` readback + reference-meter calibration; optional PAR sensor → `ppfd`. | fc band evaluable (4000-6000); dusk offset tuned to dry-down. | — |
| NB5 | P3 | Sensing/Hardware | Non-overhead night humidity source (OPN-2, highest-value) | operator + firmware | Evaporative pad/wet-floor/humidifier+fan + relay + night humidity controller; auto-disable CYC-4 micro-pulse when present. | Overnight VPD held without wetting; CYC-3/CYC-4 retire. | — |
| NB6 | P3 | Climate/Hardware | HAF circulation fan (ENV-6) | operator + firmware | Low-speed HAF fan + relay exempt from vent interlock; runs during wetting. | Gentle circulation whenever wetting. | — |
| NB7 | P3 | Irrigation | CYC-4 overnight ≤5s micro-pulse | firmware | Dedicated short-pulse path bypassing min_fog_on_s duty table (separate timer/duty entry), clean/dew-gated; trigger overnight VPD>ceiling 1.25; replay invariant fog ON ≤10s outside wet window. | ≤5s pulses only as last resort; auto-disabled when NB5 present. | NB5 (auto-disable) |
| NB8 | P3 | Sensing/Hardware | Feed-water TDS + temp (HW-5, IRR-7) | operator + firmware | Source TDS/EC + feed-water-temp probe; SAF guard (suspend if <50F or >TDS threshold). | Tepid/low-TDS verified. | — |
| SEA1 | P3 | Climate | Dormancy phase + dormant lighting clamp + dry-down governance (SEA-1/SEA-3) | coordinator + firmware + ingestor | Dormancy flag + dormant orchid rows (provisional); dormant lighting clamp 11-13h; dry-down governor (velamen/VPD-recovery proxy — Vanda has no soil probe). | Dormant set serves only after OPN-4 empirical confirmation. | OPN-4 observations |
| W1 | P3 | Public/Site | site-doctor exits 1 on stale soil.md; RAG 6d stale (B18) | web | Fix 252h-stale soil.md served live; refresh site_content RAG. | site-doctor green; RAG fresh. | T1 (topology truth) |
| SL1 | P3 | Topology/Slack | Slack `crop.feed` + `shade.event` + topology.confirm intents; set crop_catalog_id on create (B2/B15/TOPO-5) | ingestor/genai + coordinator | Add feed/shade/topology-confirm intents (write crop_events/observations w/ EC+volume+time); `_crop_create` resolves+sets crop_catalog_id; tie to crop_tasks/checklist follow-up. | Physical work (feed/shade/repot) logged via Slack; new plantings join band; south Canna "dying" read followed up. | — |
| SL2 | P3 | Topology/Slack | Optional `patio` pseudo-position | coordinator | Add external/patio position so seasonal moves transplant (preserve record) vs clear. | Canna can return in fall without re-create. | — |

---

## Band + Compliance Rearchitecture (decisions #1–#3)

Companion design: `docs/design/band-compliance-architecture.md`. **Strict layering:** the band-source fix is **migration 145** (rows D1/D2/D3/B0a/B0b/T1 above — a hard dependency). The compliance rearchitecture is **migration 146** (graded + per-zone + feasibility; additive, dual-write, no reward change) followed by **migration 147** (the deliberate reward swap). All DB objects (`fn_*`, views, `daily_summary` columns, new tables, `verdify_schemas`) are **shared territory → coordinator**. None of this touches the live control path (`setpoint-server.py:309-311`), so no firmware OTA / no replay-diff block / no 48h bake — attach `make firmware-replay` showing 0% divergence as the DB-only artifact (rule 8).

Decisions locked by operator (2026-05-29): **#1 SPATIAL=HYBRID** (per-zone grading bands; single served line follows the Vanda center; house score = aggregate of per-zone compliance; temp grading is compliance-only). **#2 COMPLIANCE=GRADED+FEASIBILITY-AWARE** (full credit in ideal, linear partial in stress, 0 beyond; decompose misses into controller-error vs physically-unachievable; emit raw + controller-attributable). **#3 TARGET=BLEND** (anchor on declarative `crop_target_profiles`, NOT empirical p25–p75; clamp/widen to a per-season physically-achievable envelope).

### Prerequisite (sequence INTO migration 145, before any season-dependent function)

| ID | Pri | Theme | Item | Owner | Concrete action | Acceptance | Depends-on |
|---|---|---|---|---|---|---|---|
| D0 | P0 | Data/DB | Fix `fn_current_season()` volatility (latent correctness bug) | coordinator | `ALTER FUNCTION fn_current_season() STABLE` — verified declared `IMMUTABLE` (`provolatile='i'`) but reads `now()`, so it may constant-fold to a stale season across the June-1 spring→summer flip. Sequence into migration 145 before D1/D3 (and before 146's season JOIN / `fn_achievable_envelope`). | `pg_proc.provolatile='s'`; season switches correctly at the June-1 boundary; all season-aware bands serve summer rows after 2026-06-01. | — (into mig 145, first) |

### Migration 145 additions — per-zone GRADING band + achievable envelope (extend mig 145 transaction)

These extend migration 145's transaction (Vanda design §5.1 ordering): helpers before callers; B0a/B0b/T1/D2 before any catalog-join; `_default` rows before `fn_zone_band`; `v_target_curve` repoint before any deprecation.

| ID | Pri | Theme | Item | Owner | Concrete action | Acceptance | Depends-on |
|---|---|---|---|---|---|---|---|
| D4 | P0 | Climate/Bands | `fn_zone_band(zone,ts)` — per-zone GRADING band (ideal+stress) | coordinator | New STABLE fn emitting per-zone ideal+stress band via catalog/`is_active`/season join: center→orchid; east→intersection(ideal)/union(stress) over lettuce∩strawberry∩pepper (verified h14: ideal_high 78, stress_high 95, vpd_stress_high 2.5); empty zones→`_default`. Reads whatever D2 sets. | `fn_zone_band('east',ts)` returns ideal 78 / stress 95 at h14; center→orchid; empty→`_default`; never NULL. | B0a, B0b, T1, D2, D6 |
| D5 | P0 | Climate/Bands | `fn_achievable_envelope` + table + `fn_active_noncenter_stress`; wrap `fn_band_setpoints` | coordinator (table+fn+clamp) / ingestor (refresh job) / genai (concession→planner) | `CREATE TABLE achievable_envelope` (+`env_temp_achievable_p50`, provenance jsonb incl `residual_stdev`); `fn_achievable_envelope(zone,season,ts)` accessor (interp + nearest-season fallback, never NULL). `refresh_achievable_envelope` job: Term A (`outdoor_p50 + k·solar + margin`, **k≈0.0035 corrected, margin 2.0**) max Term B (`indoor_p90` saturated-only, **gated `n_hot_samples≥20` at outdoor≥80F** — current saturated data is spring-biased) **min agronomic-relative cap (`agro_ideal − overheat_slack`)** so the clamp is never an identity on ≥90F days. Wrap `fn_band_setpoints` with envelope clamp + `fn_active_noncenter_stress` floor/ceiling (decision #1). Emit `agronomic_*`+`concession_*` shadow cols. `hardware_epoch` gate for shade re-fit. | Served center midday cap derived (worked: ~90F summer, provenance auditable), NOT hand-set; clamp fails open-to-achievable on missing season; `concession_temp_f` surfaced as "tolerated, not ideal"; dispatcher lookup <5ms. | D1, D4 (sibling to 145) |
| D6 | P0 | Climate/Bands | Shared `fn_diurnal_interp` + `fn_solar_sunrise_hour/sunset_hour` (SEA-2) | coordinator | Extract mig-145 §3.4 cos² math into `fn_diurnal_interp(ts,night,day)` so grading band == served line shape; add thin IMMUTABLE zero-finders over `fn_solar_altitude` (verify no `now()`/mutable state). Refactor `fn_center_band_setpoints`/`fn_band_setpoints` onto it. | One shared shape engine; sunrise/sunset seasonal; all IMMUTABLE. | D1 |
| D7 | P1 | Climate/Bands | `crop_target_profiles` `_default` rows (spring+summer) + `v_zone_band` surface | coordinator | INSERT non-joined `_default` 24h rows (ideal 60–80/stress 45–95, vpd 0.4–1.4/0.2–2.0) for empty zones; `v_zone_band` thin view over `fn_zone_band` for all zones at `now()` (compliance + dashboards). | Empty zones grade against `_default`; `v_zone_band` populated for all zones. | B0b, D4 |
| D8 | P1 | Climate/Bands | Deprecate `fn_target_band_smooth`/`fn_target_band`; **repoint `v_target_curve` FIRST** | coordinator + web | `v_target_curve` DEPENDS on `fn_target_band_smooth` (verified pg_depend, 289 rows — NOT orphaned). Repoint `v_target_curve`→`v_zone_band`, THEN COMMENT-deprecate + drop. CI grep guard on Python/SQL migration files only (exclude `db/schema.sql`, `docs/`, 145/146 migrations). | `v_target_curve` repointed; functions dropped without CASCADE surprise; dashboards intact. | D7 |

### Migration 146 — graded + feasibility-aware compliance engine (after 145; additive, dual-write)

146 reads 145's served band + re-authored orchid stress (D2). A `RAISE EXCEPTION` guard hard-blocks 146 if D2 not applied. No reward number moves in 146.

| ID | Pri | Theme | Item | Owner | Concrete action | Acceptance | Depends-on |
|---|---|---|---|---|---|---|---|
| G1 | P1 | Compliance | `fn_grade_credit` graded piecewise scalar (decision #2) | coordinator | IMMUTABLE: 1.0 in ideal, linear `(x−stress_lo)/(ideal_lo−stress_lo)` / `(stress_hi−x)/(stress_hi−ideal_hi)` in shoulder, 0 beyond (NULLIF/GREATEST guards). | Continuous at all band edges; g∈[0,1]; verified vs orchid h14 profile. | mig 145 (D2 stress bounds), D4 |
| G2 | P1 | Compliance | `fn_zone_band_grade(start,end)` + matview (replaces `v_setpoint_compliance`; resolves M11) | coordinator | Extend the EXISTING time-bounded `fn_band_trace` (NOT the 5-UNION `v_setpoint_compliance`): per-minute × per-zone {center/east/north} graded credit + geometric-mean zone_score + feasibility label. Materialize as **CAgg directly on the `climate` hypertable with a regular-table JOIN to `crop_target_profiles`** (TS 2.25.2 — CAgg-on-view + LATERAL-to-fn is INVALID) OR a `pg_cron` matview; feasibility in a thin downstream rollup. | Per-zone graded compliance computes <1s over recent window; `v_setpoint_compliance` >120s timeout retired (M11 closed). | G1 |
| G3 | P1 | Compliance | Feasibility decomposition + `fn_compliance_v2`/`fn_house_compliance` rollups | coordinator + genai | Per-miss classifier: HOT unachievable if vent ON & outdoor≥target (or both fans ON & outdoor≥indoor); COLD unachievable if both heaters ON; **VPD-HIGH unachievable only if vent ON AND (temp_band_error>0 OR outdoor≥target)** [TIGHTENED — 519/6611 (7.9%) vent-while-temp-OK are controller-error, not unachievable]; pre-2026-05-24 → `feasibility_unknown`. Emit raw + controller-attributable; `unachievable_frac`. House roll-up via priority weights. Re-point `gather-plan-context.sh:221` `fn_compliance_pct`→`fn_compliance_v2`. | `fn_compliance_v2('6h')` returns per-zone raw+ctrl+unachievable_frac; ctrl ≥ raw; reproduces 73.4% hot-unachievable. | G2 |
| G4 | P1 | Compliance | `daily_summary` graded/ctrl/feasibility cols + `daily_zone_compliance` + `compliance_zone_weights` | coordinator | ALTER daily_summary ADD `compliance_v2_raw_pct`/`_attributable_pct`/`_unachievable_frac`, graded temp/vpd + 4 graded_stress_hours + feasibility_unknown_min (existing binary cols NOT mutated). `daily_zone_compliance` child table (per date×zone, proxy_flag). `compliance_zone_weights` seeded **center 0.60 / east 0.40 / north,south,west 0** (empty zones excluded from house reward — resolves the null-crop-weight gap). | New columns/tables present; weights table seeded; binary cols byte-stable. | G1 |
| G5 | P1 | Compliance | `tasks.py` loop: graded+feasibility in the EXISTING per-reading loop, co-exist with binary | ingestor | Extend readings SELECT (per-zone + outdoor inputs); build per-zone profile-band timeline once/day (O(72)); compute graded credit + feasibility from the forward-filled relay state (reuse L5050–5108); accumulate alongside untouched binary; extend UPDATE + INSERT daily_zone_compliance. | Dual-write `*_v2` every cycle, sub-second per day, no extra climate scan; binary columns unchanged. | G4 |
| G6 | P1 | Compliance | Retire 3rd stress path; re-point ALL `v_stress_hours_today`/`fn_stress_summary` consumers | ingestor + coordinator + web | Re-point to graded-deficit hours: `alert-monitor.py:248`, **`tasks.py:1271` (2nd VPD check)**, **grafana `operations.json:5119`** (`fn_stress_summary`), **grafana `greenhouse-owner-overview.json:1090`** (`v_stress_hours_today`), **`tests/test_02_database.py` 34/83/164–180** asserts. Recalibrate VPD-high alert threshold to `max(0.5, p75 rolling-30d graded center vpd_high)` (the 2.0h binary threshold goes unreachable post-145). | All 5 consumer locations re-pointed; alert still fires sensibly; suite green. | G5 |

### Migration 147 — planner reward swap (after Phase 2 validation gate)

| ID | Pri | Theme | Item | Owner | Concrete action | Acceptance | Depends-on |
|---|---|---|---|---|---|---|---|
| G7 | P0-after | Reward | Re-point reward to `compliance_v2_attributable_pct` + re-anchor the ladder | coordinator + genai | Re-point `v_daily_kpi`/`v_planner_performance`/`fn_plan_anchor_score` to the attributable column (keep `comp/100*80+cost*20` form — drop-in). **Re-anchor the 9/10-tier ladder by quantile-match** so the 103 anchored plans preserve rank-order+median anchor. **Freeze the 103 anchored + 228 outcome rows**; backfill only the 2 null/in-flight (optionally the 125 outcome-no-anchor) on the new scale — do NOT re-run `backfill-plan-evaluations.py` over history. Update prompt copy `iris_planner.py:189/198/217` + `mcp/server.py:373`. Bounce verdify-mcp + verdify-ingestor (rule 7). | ≥90% of 103 anchored plans reproduce their anchor (ordinal stability); deviation-guard trip-count does not spike; binary columns kept 1 cycle for rollback. | G5 (co-existence window elapsed + Phase 2 gate) |
| G8 | P1 | Schema | `verdify_schemas` graded/attributable/per-zone fields + drift guards (schema-first) | coordinator | Add `compliance_v2_*`/per-zone fields to `views.py`/`mcp_responses.py`/`daily.py`; **`ScorecardResponse` (extra='forbid') must accept new keys BEFORE `fn_planner_scorecard` emits them**; add belt-and-suspenders try/except at `api/main.py:1396` (`/api/v1/scorecard`, also `:2282`/`:2436` unguarded). Add drift-guard tests; repair M9's 6 rotted tests in the same window. Lands ONE cycle ahead of consumers. | New fields in contracts; public scorecard endpoint cannot 500 on a new key; drift guards green. | G4 (schema-first, before G7 consumers) |
| G9 | P2 | Display | Web relabel: Grafana / public baseline / Prometheus / evidence / dataset | web | Re-run live Grafana audit (`audit-grafana.py`) — the "~13 dashboards / plant-health.json" inventory is STALE. Relabel "both inside the firmware-enforced band" (false once graded+per-zone) on `generate-baseline-vs-iris-page.py:436`, `verdify-metrics.py:69` HELP, evidence snapshots, public dataset export; add graded/attributable panels; keep legacy binary under its old name. | Public labels accurate (evidence-integrity); legacy continuity preserved. | G4 |

---

### Notes on sequencing & gates

- **Migration 145** bundles **D0** (fn_current_season STABLE, FIRST), T1, B0a, B0b, D1, D2, D3, N1, S1(view), **plus the band-compliance band-layer additions D6 (shared interp+solar helpers), D4 (per-zone grading band), D5 (achievable envelope + clamp wrap), D7 (`_default` rows + `v_zone_band`)**, in the strict internal order in design §5.1 (helpers before callers; B0a/B0b/T1/D2 before any catalog-join; `_default` before `fn_zone_band`). **D8** (deprecate `fn_target_band_smooth`/`fn_target_band` — repoint `v_target_curve`→`v_zone_band` first) lands here or the next cycle. Restart: bounce **setpoint-server (dispatcher)** + **verdify-mcp**. No firmware OTA. Setpoint-policy change → attach `make firmware-replay` + coordinator THRESHOLD_PCT sign-off (rule 8).
- **Migration 146 (compliance engine)** lands strictly AFTER 145 (a `RAISE EXCEPTION` guard blocks it until D2's orchid stress bounds are applied): G1–G6 (graded scalar, `fn_zone_band_grade` extending `fn_band_trace`, feasibility decomposition, `fn_compliance_v2`/`fn_house_compliance`, daily_summary `*_v2` cols + `daily_zone_compliance` + `compliance_zone_weights`, the `tasks.py` dual-write loop, retire the 3rd stress path). **Additive + dual-write — no reward number moves.** DB-only (off `setpoint-server.py:309-311`) → no OTA, no replay-diff block, no 48h bake; attach `make firmware-replay OLD=145-base NEW=146` showing 0% divergence. Restart: bounce **verdify-ingestor** + **verdify-mcp**. Closes **M11** (replaces `v_setpoint_compliance` rather than indexing it).
- **Migration 147 (reward swap)** lands AFTER the Phase-2 validation gate (≥90% of the 103 anchored plans reproduce their anchor under the re-anchored ladder; deviation-guard trip-count does not spike): G7 re-points the reward to `compliance_v2_attributable_pct` + re-anchors the ladder + freezes the 103 anchored / 228 outcome rows + rewrites the planner prompt copy. **G8 (verdify_schemas + ScorecardResponse) is schema-first — lands one cycle ahead of G7 consumers** so the public `/api/v1/scorecard` cannot 500 on a new metric key. G9 (web relabel) follows in Phase 4. Bounce **verdify-mcp** + **verdify-ingestor**.
- **C1 (econ-heat guard)** must ship AFTER D2 (the 0.75 night VPD floor activates that heat path); it is a firmware OTA → replay-diff + invariants + 48h bake + ≤1 OTA/week, no deploy with any open critical alert (rules 1-4, 8-9). Every new tunable needs a `cfg_*` readback (rule 6).
- **C2 dispatcher half** (sunset cutoff push) ships first with no OTA and fixes the live CYC-1 violation immediately; the firmware stress-window cap follows in the next OTA.
- **June-1 deadline:** B0b (summer rows) + D1/D3 (`fn_current_season()`) must land before 2026-06-01 or `fn_band_setpoints` serves stale spring data (and, if the season-switch lands without summer rows, NULL). Treat as P0 hard deadline.
- **Hardware P0/P1 (H1 shade, NB1 center VPD, NB2 center fert mister)** are operator-gated; H1 (shade) is the only real mitigation for the 94-105F June cluster — escalate now.

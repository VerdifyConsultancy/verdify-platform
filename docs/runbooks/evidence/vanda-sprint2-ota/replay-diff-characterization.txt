Replay-diff characterization — Vanda sprint-2 OTA (center-zone moisture-guardrail)
=================================================================================
Branch:   firmware/vanda-band-compliance-rearch
HEAD:     e7781a3 (committed last-night bundle) + UNCOMMITTED worktree (center-guardrail)
Built:    2026-05-30 ~10:12 MDT (firmware-check SUCCESS, config_hash=0xbed099a9)
Corpus:   firmware/test/data/replay_overrides.csv.gz — 193,525 rows

TWO DIFFS WERE RUN TO ISOLATE THE CHANGE
----------------------------------------
A) worktree vs main (fb17f43):    4,919 / 193,525 = 2.54% divergent   (exit 2, expected*)
B) worktree vs e7781a3 (bundle):  0 / 193,525    = 0.00% divergent   (exit 0)

* Exit 2 is the EXPECTED result under the default THRESHOLD_PCT=0 (CLAUDE.md rule 8):
  this is an intentional-divergence firmware change that requires operator
  THRESHOLD_PCT sign-off. It is NOT a failure.

ISOLATION CONCLUSION (the operator sign-off basis)
--------------------------------------------------
Diff B = 0 rows proves the center-zone moisture-guardrail change (this sprint's
ONLY firmware edit) contributes EXACTLY ZERO mode/relay divergence over the full
193,525-row corpus. This is by construction: the new center exemption ships at
the no-op default center_moisture_relax_kpa = 0.0, where

    center_engage_kpa = max(mister_engage_kpa - relax,  vpd_target_center + min_excess)
                      = max(mister_engage_kpa - 0,       0.8 + 0.05)
                      = max(1.6, 0.85) = 1.6 = mister_engage_kpa  (exactly)

so center_stressed ⊆ global_stressed and any_zone_stressed is unchanged. The
replay corpus carries no sp_center_moisture_relax_kpa column, so the C++ replay
uses the shipped default (relax=0) on every row → identical decisions to e7781a3.

Therefore 100% of the 2.54% (diff A) is attributable to the COMMITTED last-night
bundle (e7781a3), and 0% to this sprint's center-guardrail change.

CHARACTERIZATION OF THE 2.54% (last-night bundle — econ-heat-night + dusk-cutoff + feed-hold)
---------------------------------------------------------------------------------------------
Note on replay time: replay_emit reads local_hour from the UTC hour of the ts
column (no tz conversion); hours below are replay-hours, and the new defaults
(dusk_cutoff_hour=18, night window 20→6) are active on the NEW side.

Hour distribution (all divergence in dusk + night windows + a carryover tail):
  dusk window  (h18-21): 1626 + 470 + 381 + 382 = 2,859 rows
  night window (h00-06): 737+458+214+164+128+126+42 = 1,869 rows
  carryover    (h07-12): 38+39+37+36+31+10 = 191 rows
  (total 4,919)

Net actuator effect (NEW vs OLD):
  fog  : 2,355 turned OFF,  7 turned ON
         the 7 ON = 6 SAFETY_COOL survival-cooling rows (dusk-exempt by design,
         temp >= safety_max) + 1 FSM mist re-engage at the night-window end.
  heat1: 1 turned ON, 0 OFF — legitimate low-temp BAND heating exposed when a
         futile night dry-stress VENTILATE correctly drops to IDLE (NOT econ-rescue
         heat; the econ path is suppressed at night).
  heat2: 0 changes.  fan1: 1 OFF.  fan2: 0.  vent: 1 OFF.
  No daytime cooling/vent/fan control changed except where dusk/night gates apply.

Mode transitions (OLD→NEW):
  SEALED_MIST→IDLE      2,857  (wetting suppressed past dusk / at night)
  VENTILATE→VENTILATE   1,896  (fog dropped within an otherwise-unchanged vent decision)
  IDLE→SEALED_MIST         80  } mist-stage machine resync after a diverged night
  SEALED_MIST→SEALED_MIST  79  }
  SAFETY_COOL→SAFETY_COOL   6  (survival fog now firing — dusk-exempt)
  VENTILATE→IDLE            1

Interpretation: the intended dry-down behavior — ~2,355 fewer fog-minutes
overnight/at-dusk, no overnight heat-to-chase-humidity, no daytime control change.
This matches the prior (e7781a3) bundle characterization row-for-row, as expected
since diff B = 0.

OPERATOR THRESHOLD_PCT RECOMMENDATION
-------------------------------------
Recommend THRESHOLD_PCT=3 (covers the 2.54%) for the merge/OTA of this branch.
The divergence is entirely the already-reviewed last-night bundle; the sprint-2
center-guardrail change adds 0%.

INVARIANTS
----------
make firmware-invariants → 0 violations over 193,525 rows, all 21 invariants
(16 Phase-0 core + #17-#21 Vanda band-compliance incl. the NEW #21
center_relax_bounded). NOTE: the summary line still prints "All 16 invariants
passed" — a cosmetic count-string left at 16 in replay_invariants.cpp:326 since
the bundle added #17-#21; the runner DOES execute and gate all 21 (returns the
violation count). Recommend updating that literal to 21 in a follow-up; it is not
a gate defect (0 violations either way).

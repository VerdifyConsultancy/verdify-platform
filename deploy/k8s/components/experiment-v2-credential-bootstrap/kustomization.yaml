apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Gate-2 credential activation. Migration waves create and normalize the six
# passwordless experiment LOGIN identities; this PreSync hook installs their
# protected SCRAM verifiers atomically and proves every exact login/duty/ACL
# boundary over a new TCP connection while the experiment capability is off.
resources:
  - bootstrap-job.yaml

images:
  # The consuming production overlay rewrites this logical key to the same
  # immutable migrate digest used by the migration and ordinary-role hooks.
  - name: ghcr.io/verdifyconsultancy/verdify-migrate

apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Kustomize COMPONENT: the verdify-planner workload (#117). Referenced by the
# live prod overlay via `components:` — deliberately NOT a base resource, so
# future overlays must opt in explicitly. See planner-deployment.yaml header for
# the full rationale. The image (ghcr.io/verdifyconsultancy/verdify-planner) is
# pinned to a @sha256 digest by the consuming overlay's images: transformer.
resources:
  - planner-deployment.yaml
